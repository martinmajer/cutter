<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{$title}</title>
    </head>
    <body class="{$bodyClass}">
        {if $showSomething}
            {(function() {return "}}\"foo\"{{";}())}
        {else}
            {foreach key in $data}
                {@$data[key]}
            {/}
        {/}
    </body>
</html>

